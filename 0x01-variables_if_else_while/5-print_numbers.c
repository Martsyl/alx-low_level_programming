#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int numbase;

	for (numbase = 0; numbaese < 10; numbase++)
		printf("%d", numbase);
	printf('\n');
	return (0);
}
