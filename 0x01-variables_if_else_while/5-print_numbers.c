#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int numbase;

	for (numbase = 0; numbase < 10; numbase++)
		printf("%d \n", numbase);
	return (0);
}
